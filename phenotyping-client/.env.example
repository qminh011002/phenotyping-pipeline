# Copy to .env (or .env.local) and adjust per environment.
# Vite exposes only VITE_-prefixed vars to the browser bundle — do not place secrets here.

# Backend API base URL used by src/services/http.ts (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000
